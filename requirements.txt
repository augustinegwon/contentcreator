# YouTube Data API v3 를 파이썬에서 호출하기 위한 공식 라이브러리
google-api-python-client>=2.100.0

# .env 파일에서 환경변수(API 키 등)를 읽어오기 위한 라이브러리
python-dotenv>=1.0.0

# 엑셀(.xlsx) 파일을 만들기 위한 라이브러리 (view.py 에서 사용)
openpyxl>=3.1.0

# 웹앱(브라우저에서 쓰는 화면)을 띄우기 위한 라이브러리 (app.py 에서 사용)
Flask>=3.0.0
